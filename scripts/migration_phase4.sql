--==============================================================================
-- migration_phase4.sql — 이미지 Vision 태깅 결과 저장 컬럼 추가
--
-- [배경] CLIP은 이미지 벡터 유사도 검색만 할 뿐, 이미지의 의미(색상/소재/스타일)를
-- 텍스트로 뽑아내진 못한다. Vision LLM(gpt-4o-mini)으로 상품 이미지를 분석해
-- 태그/설명을 생성하고, 이 컬럼에 저장한 뒤 텍스트 임베딩(RAG)·BM25 색인 시
-- DESCRIPTION과 합쳐서 사용한다 — 이미지 검색뿐 아니라 텍스트 검색 정확도도 향상.
--==============================================================================

ALTER TABLE PRODUCT ADD IMAGE_CAPTION VARCHAR2(500);
